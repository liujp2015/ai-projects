# 下一步操作指南

## ✅ 已完成的工作

1. ✅ 项目结构已创建
2. ✅ 所有代码文件已生成
3. ✅ 环境变量模板已创建
4. ✅ 安装脚本已创建

## 📋 现在需要执行的操作

### 步骤1: 安装依赖（必须）

#### Windows PowerShell:

```powershell
# 在项目根目录执行
cd ai/first-project
.\install.ps1
```

#### Linux/Mac:

```bash
# 在项目根目录执行
cd ai/first-project
chmod +x install.sh
./install.sh
```

#### 手动安装（如果脚本失败）:

```bash
# 后端
cd backend
npm install

# 前台
cd ../frontend
npm install

# 后台管理
cd ../admin
npm install
```

### 步骤2: 配置数据库

#### 2.1 创建PostgreSQL数据库

```sql
-- 使用psql或pgAdmin创建数据库
CREATE DATABASE rebate_db;
```

#### 2.2 修改数据库连接配置

编辑 `backend/.env` 文件，修改以下行：

```env
DATABASE_URL="postgresql://你的用户名:你的密码@localhost:5432/rebate_db"
```

**示例**:
```env
DATABASE_URL="postgresql://postgres:123456@localhost:5432/rebate_db"
```

### 步骤3: 启动Redis

确保Redis服务正在运行：

**Windows:**
- 如果已安装Redis，运行 `redis-server`
- 或使用Docker: `docker run -d -p 6379:6379 redis:latest`

**Linux/Mac:**
```bash
redis-server
# 或
sudo systemctl start redis
```

**验证Redis运行:**
```bash
redis-cli ping
# 应该返回: PONG
```

### 步骤4: 初始化数据库

#### Windows PowerShell:

```powershell
cd ai/first-project
.\init-db.ps1
```

#### 手动执行:

```bash
cd backend

# 1. 生成Prisma Client
npm run prisma:generate

# 2. 运行数据库迁移
npm run prisma:migrate
# 输入迁移名称: init

# 3. 填充种子数据（创建默认管理员）
npm run prisma:seed
```

**预期输出:**
- ✅ Prisma Client生成成功
- ✅ 数据库迁移成功（创建所有表）
- ✅ 种子数据填充成功（创建默认管理员）

### 步骤5: 启动服务

需要打开**3个终端窗口**，分别启动3个服务：

#### 终端1 - 启动后端

**Windows PowerShell:**
```powershell
cd ai/first-project
.\start-backend.ps1
```

**或手动:**
```bash
cd ai/first-project/backend
npm run start:dev
```

**成功标志:**
```
Application is running on: http://localhost:3000
```

#### 终端2 - 启动前台

**Windows PowerShell:**
```powershell
cd ai/first-project
.\start-frontend.ps1
```

**或手动:**
```bash
cd ai/first-project/frontend
npm run dev
```

**成功标志:**
```
- Local:        http://localhost:3001
```

#### 终端3 - 启动后台管理

**Windows PowerShell:**
```powershell
cd ai/first-project
.\start-admin.ps1
```

**或手动:**
```bash
cd ai/first-project/admin
npm start
```

**成功标志:**
```
  App running at:
  - Local:    http://localhost:8000
```

## 🧪 验证安装

### 1. 测试后端API

打开浏览器访问: http://localhost:3000/api/health

**预期响应:**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "status": "ok",
    "timestamp": "2024-..."
  }
}
```

### 2. 测试后台管理登录

1. 访问: http://localhost:8000/login
2. 输入账号:
   - 邮箱: `admin@example.com`
   - 密码: `admin123`
3. 点击登录

**成功标志:** 跳转到仪表盘页面

### 3. 测试前台访问

访问: http://localhost:3001

**成功标志:** 看到首页（Banner和商品列表）

## 🎯 首次使用流程

### 1. 登录后台管理

- 访问: http://localhost:8000/login
- 使用默认账号登录

### 2. 创建基础数据

#### 创建品牌
1. 点击"品牌管理"
2. 点击"添加品牌"
3. 输入品牌名称和描述
4. 保存

#### 创建分类
1. 点击"分类管理"
2. 点击"添加分类"
3. 输入分类名称
4. 保存

#### 创建商品
1. 点击"商品管理"
2. 点击"添加商品"
3. 填写商品信息：
   - 标题
   - 原价、折扣价
   - 跳转链接（重要！）
   - 选择品牌和分类
   - 购物平台
4. 保存

#### 创建Banner
1. 点击"Banner管理"
2. 点击"添加Banner"
3. 输入图片URL和链接
4. 保存

### 3. 测试前台功能

1. 访问前台首页，查看商品列表
2. 点击商品查看详情
3. 点击"去购买"测试跳转
4. 注册新用户测试注册功能
5. 登录后查看个人中心

### 4. 测试商品推送

1. 在后台管理找到商品
2. 点击"推送"按钮
3. 在前台登录的用户应该收到推送通知

## ⚠️ 常见问题

### Q1: npm install 很慢或失败

**解决方案:**
- 使用国内镜像: `npm config set registry https://registry.npmmirror.com`
- 或使用yarn: `yarn install`
- 检查网络连接

### Q2: Prisma迁移失败

**错误**: `P1001: Can't reach database server`

**解决方案:**
1. 检查PostgreSQL是否运行
2. 检查`.env`中的DATABASE_URL配置
3. 确认数据库`rebate_db`已创建
4. 检查用户名密码是否正确

### Q3: Redis连接失败

**错误**: `Error: connect ECONNREFUSED 127.0.0.1:6379`

**解决方案:**
1. 检查Redis是否运行: `redis-cli ping`
2. 检查`.env`中的REDIS配置
3. 确认端口6379未被占用

### Q4: 端口被占用

**解决方案:**
- 修改端口配置
- 或关闭占用端口的进程

**Windows查看端口占用:**
```powershell
netstat -ano | findstr :3000
```

### Q5: 模块找不到错误

**解决方案:**
1. 删除`node_modules`文件夹
2. 删除`package-lock.json`
3. 重新运行`npm install`

### Q6: TypeScript编译错误

**解决方案:**
1. 检查`tsconfig.json`配置
2. 确保所有依赖已安装
3. 运行`npm run build`查看详细错误

## 📚 相关文档

- `START.md` - 快速启动指南
- `INSTALL.md` - 详细安装说明
- `COMPLETION_SUMMARY.md` - 项目完成总结
- `PROJECT_STATUS.md` - 项目状态文档

## 🎉 完成标志

当以下所有项都成功时，说明项目已正确安装：

- ✅ 三个服务都能正常启动
- ✅ 后端API健康检查通过
- ✅ 后台管理可以登录
- ✅ 前台可以访问
- ✅ 数据库表已创建
- ✅ 默认管理员账号可以登录

## 💡 开发提示

1. **代码修改会自动重启**（watch模式）
2. **查看日志**了解运行状态
3. **使用Prisma Studio**查看数据库: `npm run prisma:studio`
4. **API测试**可以使用Postman或curl

## 🚀 开始开发

现在你可以：
1. 在后台管理添加数据
2. 在前台查看效果
3. 开始自定义开发
4. 添加新功能

祝开发顺利！🎊

